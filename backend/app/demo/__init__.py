"""The demonstration: a world of somebody's own, and the operators inside it.

Two modules, split the way the tables are. `sandboxes` owns the world — when one may be built, how
long it lives, and everything that has to be deleted when it stops. `operators` owns the rows a
sandbox needs in order to have a users screen worth opening.
"""
